export interface EpicProps {
  content: {
    header: string;
    body: string;
  };
}

export interface BannerProps {
  content: {
    header: string;
    body: string;
  };
  mobileContent: {
    header: string;
    body: string;
  };
}
